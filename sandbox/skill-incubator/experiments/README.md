# Locked Experiments

This directory intentionally contains no experiment implementation, copied
third-party code, dependencies, or runtime configuration. Future experiments
may be proposed here only after a separate, explicit approval step.
