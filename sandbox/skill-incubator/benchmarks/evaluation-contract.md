# Evaluation Contract

An experiment passes only when it preserves safety controls and improves a
predeclared metric against a fixed baseline. A popularity statistic, screenshot,
or vendor claim is not a benchmark result.

Required evidence: immutable source pin, isolated environment, test inputs,
expected outcomes, failure analysis, rollback plan, and explicit customer review.
