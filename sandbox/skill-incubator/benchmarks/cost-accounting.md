# Cost Accounting

Record actual input, output, and cached tokens; model and tool calls; elapsed,
CPU, GPU, and human-review time; peak memory; disk writes; network bytes;
provider and API charges; and failed-run cost. Use `not_available` when an
actual metric cannot be obtained. Never present an estimate as measurement.

Any external cost, provider call, or paid service needs separate approval before
an experiment begins.
