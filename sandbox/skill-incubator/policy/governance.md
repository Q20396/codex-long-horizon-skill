# Governance

Every source and experiment begins with `status: locked`. A proposal is not
approval, and approval is not permission to install, execute, import, publish,
or promote. Each future action needs a separate explicit customer decision.

Only immutable GitHub commit SHAs may be used as an experiment baseline.
Branches, moving tags, `main`, and `latest` are discovery references only.
