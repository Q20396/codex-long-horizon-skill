# Prompt

Use the long-horizon-engineering skill in guided customer mode.

Our operations team manually combines three CSV exports each Friday. I want to
decide whether we should authorize a local weekly-summary prototype.

You may read only `docs/import-process.md` and
`tests/fixtures/synthetic-weekly-export.csv`. The fixture is synthetic.
Do not read production exports, write files, use the network, install packages,
or contact an external service. Stop if the parser source or real customer data
is required.

Give me the customer outcome first. Put scope controls and engineering evidence
in separate layers. End with exactly one next safe action and leave approval to
me.
