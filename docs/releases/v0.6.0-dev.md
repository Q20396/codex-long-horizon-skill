# Long-Horizon Engineering v0.6.0-dev

Release date: 2026-08-02

Release state: candidate

This candidate covers long-horizon-engineering and AI video production source
development. The repository marketplace remains pinned to the immutable
`v0.5.0` Codex plugin release, so this candidate is neither an installation
target nor a release authorization.

Validation is limited to candidate package and release-readiness checks. It
does not authorize customer-data transfer, accounts, external providers, or
runtime execution.

The candidate formal inventory contains 24 Draft 2020-12 schemas: 8 schemas
have explicit positive and negative instance fixtures, while the remaining 16
are explicitly syntax-only. This records the current candidate only; it does
not rewrite the immutable v0.5.0 release evidence.
